<p style="color: green"><%= notice %></p>

<%= render @exam %>

<div>
  <%= link_to "Edit this exam", edit_admin_exam_path(@exam) %> |
  <%= link_to "Back to exams", admin_exams_path %>

  <%= button_to "Destroy this exam", admin_exam_path(@exam), method: :delete, data: { confirm: "Are you sure?" } %>
</div>
